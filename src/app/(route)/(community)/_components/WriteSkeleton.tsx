import { Skeleton } from "@heroui/react";

const WriteSkeleton = () => {
  return (
    <div>
      <Skeleton className="w-full h-[40px] rounded-[5px] mb-[16px]" />
    </div>
  );
};

export default WriteSkeleton;
